#include <stdio.h>
#include <stdlib.h>
 
int main(void){
    //Variáveis
    int numero1, numero2, numero3;
    //Quais são as entradas de dados?
    printf("Digite um número inteiro:");
    scanf("%d", &numero1);
    fflush(stdin);
     printf("Digite outro número inteiro:");
    scanf("%d", &numero3);
    fflush(stdin);
     printf("Digite o último número inteiro:");
    scanf("%d", &numero3);
    fflush(stdin);
    //Processamento e saída de dados:
    
    return 0;
}