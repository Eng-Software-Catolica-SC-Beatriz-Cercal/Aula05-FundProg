#include <stdio.h>
#include <stdlib.h>
 
int main(void){
    //Variáveis
    int numero;
    //Quais são as entradas de dados?
    printf("Digite um número:");
    scanf("%d", &numero);
    fflush(stdin);
    //Processamento e saída de dados:
    
    return 0;
}